import {requireStaff} from "@/lib/admin/auth";import {createClient} from "@/lib/supabase/server";import {AdminUploadField} from "@/components/admin-upload-field";import {ProductAdvancedEditor} from "@/components/product-advanced-editor";import {deleteProduct,saveProduct} from "./actions";
type Props={searchParams:Promise<{error?:string;saved?:string;deleted?:string}>};
type Option={id:number;brand_id?:number;name:string};type Translation={locale:"fa"|"en";name:string;slug:string;short_description:string|null;description:string|null;key_specification:string|null;seo_title:string|null;seo_description:string|null};type Spec={locale:"fa"|"en";label:string;value:string;position:number};type Product={id:number;brand_id:number;category_id:number;sku:string|null;image_url:string|null;datasheet_url:string|null;is_featured:boolean;is_published:boolean;position:number;translations:Translation[];specs:Spec[]};
export default async function ProductsPage({searchParams}:Props){const[{profile},query]=await Promise.all([requireStaff(),searchParams]);const supabase=await createClient();const[{data:brandRows=[]},{data:brandNames=[]},{data:categoryRows=[]},{data:categoryNames=[]},{data:productRows=[]}]=await Promise.all([supabase.from("brands").select("id"),supabase.from("brand_translations").select("brand_id,name").eq("locale","fa"),supabase.from("product_categories").select("id,brand_id").order("position"),supabase.from("product_category_translations").select("category_id,name").eq("locale","fa"),supabase.from("products").select("id,brand_id,category_id,sku,image_url,datasheet_url,is_featured,is_published,position").order("position")]);const ids=(productRows??[]).map(i=>i.id);const[{data:translations=[]},{data:specRows=[]}]=ids.length?await Promise.all([supabase.from("product_translations").select("product_id,locale,name,slug,short_description,description,key_specification,seo_title,seo_description").in("product_id",ids),supabase.from("product_specifications").select("product_id,locale,label,value,position").in("product_id",ids).order("position")]):[{data:[]},{data:[]}];const brands:Option[]=(brandRows??[]).map(i=>({id:i.id,name:brandNames?.find(n=>n.brand_id===i.id)?.name||`برند ${i.id}`}));const categories:Option[]=(categoryRows??[]).map(i=>({id:i.id,brand_id:i.brand_id,name:categoryNames?.find(n=>n.category_id===i.id)?.name||`دسته ${i.id}`}));const products:Product[]=(productRows??[]).map(i=>({...i,translations:(translations??[]).filter(t=>t.product_id===i.id),specs:(specRows??[]).filter(s=>s.product_id===i.id)}));return <main className="admin-module-page admin-products-page"><header><div><div><small>کاتالوگ</small><h1>محصولات</h1><p>اطلاعات محصول و مشخصات فنی</p></div></div></header>{query.error?<p className="admin-alert error">{query.error}</p>:null}{query.saved?<p className="admin-alert success">محصول ذخیره شد.</p>:null}{query.deleted?<p className="admin-alert success">محصول حذف شد.</p>:null}<section className="admin-product-stats" aria-label="آمار محصولات"><article><small>کل محصولات</small><strong>{products.length}</strong><span>رکورد ثبت‌شده</span></article><article><small>منتشرشده</small><strong>{products.filter(item=>item.is_published).length}</strong><span>قابل مشاهده در سایت</span></article><article><small>محصولات ویژه</small><strong>{products.filter(item=>item.is_featured).length}</strong><span>نمایش برجسته</span></article><article><small>برندهای فعال</small><strong>{brands.length}</strong><span>در کاتالوگ محصولات</span></article></section><section className="admin-brand-create"><header><h2>افزودن محصول</h2></header>{brands.length&&categories.length?<ProductForm brands={brands} categories={categories}/>:<p className="admin-category-prerequisite">ابتدا برند و دسته محصول را ثبت کنید.</p>}</section><section className="admin-brand-list"><header><h2>محصولات ثبت‌شده</h2><span>{products.length}</span></header>{products.length?products.map(product=>{const fa=product.translations.find(t=>t.locale==="fa"),en=product.translations.find(t=>t.locale==="en");return <details className="admin-brand-row admin-product-row" key={product.id}><summary><span className="admin-product-thumb">{product.image_url?<span style={{backgroundImage:`url(${product.image_url})`}}/>:"DST"}</span><div><strong>{fa?.name||product.sku||"محصول"}</strong><small dir="ltr">{en?.name||product.sku||"—"}</small></div><i className={product.is_published?"published":""}>{product.is_published?"منتشرشده":"پیش‌نویس"}</i><b>ویرایش</b></summary><ProductForm brands={brands} categories={categories} product={product}/>{profile.role!=="seo"?<form action={deleteProduct} className="admin-brand-delete"><input type="hidden" name="id" value={product.id}/><button>حذف محصول</button></form>:null}</details>}):<div className="admin-catalog-empty"><strong>هنوز محصولی ثبت نشده است</strong></div>}</section></main>}
function ProductForm({brands,categories,product}:{brands:Option[];categories:Option[];product?:Product}){const fa=product?.translations.find(t=>t.locale==="fa"),en=product?.translations.find(t=>t.locale==="en");return <form action={saveProduct} className="admin-catalog-form admin-product-form">{product?<input type="hidden" name="id" value={product.id}/>:null}<label><span>برند</span><select name="brand_id" required defaultValue={product?.brand_id}>{brands.map(i=><option key={i.id} value={i.id}>{i.name}</option>)}</select></label><label><span>دسته محصول</span><select name="category_id" required defaultValue={product?.category_id}>{brands.map(brand=><optgroup label={brand.name} key={brand.id}>{categories.filter(i=>i.brand_id===brand.id).map(i=><option key={i.id} value={i.id}>{i.name}</option>)}</optgroup>)}</select></label><label><span>کد کالا</span><input name="sku" dir="ltr" defaultValue={product?.sku??""}/></label><label><span>ترتیب نمایش</span><input name="position" type="number" min="0" defaultValue={product?.position??0}/></label><AdminUploadField name="image_url" label="تصویر شاخص کارت محصول" folder="products" accept="image/jpeg,image/png,image/webp,image/avif" defaultValue={product?.image_url??""}/><input type="hidden" name="datasheet_url" value={product?.datasheet_url??""}/><ProductLocale locale="fa" value={fa} specs={product?.specs}/><ProductLocale locale="en" value={en} specs={product?.specs}/><ProductAdvancedEditor productId={product?.id}/><div className="admin-product-checks"><label><input type="checkbox" name="is_featured" defaultChecked={product?.is_featured}/><span>محصول ویژه</span></label><label><input type="checkbox" name="is_published" defaultChecked={product?.is_published}/><span>نمایش در وب‌سایت</span></label></div><button>{product?"ذخیره همه اطلاعات محصول":"ثبت کامل محصول"}</button></form>}
function ProductLocale({locale,value:translation,specs}:{locale:"fa"|"en";value?:Translation;specs?:Spec[]}){const fa=locale==="fa";const specText=specs?.filter(s=>s.locale===locale).sort((a,b)=>a.position-b.position).map(s=>`${s.label} | ${s.value}`).join("\n");return <fieldset dir={fa?"rtl":"ltr"}><legend>{fa?"محتوای فارسی":"English content"}</legend><label><span>{fa?"نام محصول":"Product name"}</span><input name={`${locale}_name`} required defaultValue={translation?.name}/></label><label><span>{fa?"نشانی":"Slug"}</span><input name={`${locale}_slug`} dir="ltr" required pattern="[a-z0-9]+(?:-[a-z0-9]+)*" defaultValue={translation?.slug}/></label><label className="wide"><span>{fa?"توضیح کوتاه":"Short description"}</span><textarea name={`${locale}_short`} rows={2} defaultValue={translation?.short_description??""}/></label><label className="wide"><span>{fa?"توضیحات کامل":"Full description"}</span><textarea name={`${locale}_description`} rows={5} defaultValue={translation?.description??""}/></label><label><span>{fa?"مشخصه کلیدی":"Key specification"}</span><input name={`${locale}_key_spec`} defaultValue={translation?.key_specification??""}/></label><label><span>{fa?"عنوان سئو":"SEO title"}</span><input name={`${locale}_seo_title`} defaultValue={translation?.seo_title??""}/></label><label className="wide"><span>{fa?"توضیحات سئو":"SEO description"}</span><textarea name={`${locale}_seo_description`} rows={2} defaultValue={translation?.seo_description??""}/></label><label className="wide"><span>{fa?"مشخصات فنی؛ هر خط: عنوان | مقدار":"Specifications; each line: label | value"}</span><textarea name={`${locale}_specs`} dir={fa?"rtl":"ltr"} rows={5} placeholder={fa?"ویسکوزیته | 10W-40":"Viscosity | 10W-40"} defaultValue={specText}/></label></fieldset>}
