import {requireStaff} from "@/lib/admin/auth";import {createClient} from "@/lib/supabase/server";import {saveSeo} from "../content-settings-actions";type Props={searchParams:Promise<Record<string,string|undefined>>};
export default async function SeoPage({searchParams}:Props){const[,q]=await Promise.all([requireStaff(),searchParams]),{data=[]}=await(await createClient()).from("seo_settings").select("id,route,locale,title,description,canonical_url,robots_index,robots_follow,og_image_url,structured_data").order("route").order("locale");return <main className="admin-module-page"><header><div><div><small>دیده‌شدن در جست‌وجو</small><h1>تنظیمات سئو</h1><p>متادیتا، ایندکس و داده ساختاریافته هر مسیر</p></div></div></header>{q.saved?<p className="admin-alert success">تنظیمات سئو ذخیره شد.</p>:null}{q.error?<p className="admin-alert error">مقادیر واردشده معتبر نیست.</p>:null}<section className="admin-brand-create"><header><h2>افزودن تنظیم مسیر</h2></header><SeoForm/></section><section className="admin-brand-list"><header><h2>مسیرهای تنظیم‌شده</h2><span>{data?.length??0}</span></header>{data?.map(item=><details className="admin-brand-row" key={item.id}><summary><div><strong dir="ltr">/{item.locale}{item.route==="/"?"":item.route}</strong><small>{item.title}</small></div><i className={item.robots_index?"published":""}>{item.robots_index?"قابل ایندکس":"Noindex"}</i><b>ویرایش</b></summary><SeoForm item={item}/></details>)}</section></main>}
function SeoForm({item}:{item?:{route:string;locale:"fa"|"en";title:string;description:string;canonical_url:string|null;robots_index:boolean;robots_follow:boolean;og_image_url:string|null;structured_data:unknown}}){return <form action={saveSeo} className="admin-catalog-form"><label><span>مسیر بدون زبان</span><input name="route" dir="ltr" required defaultValue={item?.route??"/"}/></label><label><span>زبان</span><select name="locale" defaultValue={item?.locale??"fa"}><option value="fa">فارسی</option><option value="en">English</option></select></label><label className="wide"><span>عنوان سئو</span><input name="title" required maxLength={180} defaultValue={item?.title}/></label><label className="wide"><span>توضیحات متا</span><textarea name="description" required maxLength={320} rows={3} defaultValue={item?.description}/></label><label><span>Canonical اختیاری</span><input name="canonical_url" dir="ltr" defaultValue={item?.canonical_url??""}/></label><label><span>تصویر شبکه اجتماعی</span><input name="og_image_url" dir="ltr" defaultValue={item?.og_image_url??""}/></label><label className="wide"><span>Structured data (JSON)</span><textarea name="structured_data" dir="ltr" rows={5} defaultValue={JSON.stringify(item?.structured_data??{},null,2)}/></label><div className="admin-product-checks"><label><input name="robots_index" type="checkbox" defaultChecked={item?.robots_index??true}/><span>Index</span></label><label><input name="robots_follow" type="checkbox" defaultChecked={item?.robots_follow??true}/><span>Follow</span></label></div><button>ذخیره تنظیمات سئو</button></form>}
