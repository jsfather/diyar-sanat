import {redirect} from "next/navigation";import {requireStaff} from "@/lib/admin/auth";import {roleLabel} from "@/lib/admin/modules";import {createClient} from "@/lib/supabase/server";import {createStaff} from "./actions";
export default async function StaffPage({searchParams}:{searchParams:Promise<{error?:string;created?:string}>}){const[{profile},query]=await Promise.all([requireStaff(),searchParams]);if(profile.role!=="manager")redirect("/admin");const{data=[]}=await(await createClient()).from("profiles").select("id,display_name,role,phone,is_active,last_seen_at,created_at").order("created_at",{ascending:false});return <main className="admin-module-page"><header><div><div><small>کنترل دسترسی</small><h1>کاربران و نقش‌ها</h1><p>مدیریت، ادمین و سئوکار</p></div></div></header>{query.error?<p className="admin-alert error">ایجاد کاربر انجام نشد؛ تنظیمات یا اطلاعات ورودی را بررسی کنید.</p>:null}{query.created?<p className="admin-alert success">کاربر ایجاد و فعال شد.</p>:null}<section className="admin-create-staff"><h2>ایجاد کاربر</h2><form action={createStaff}><label><span>نام نمایشی</span><input name="display_name" required/></label><label><span>ایمیل</span><input name="email" type="email" dir="ltr" required/></label><label><span>رمز اولیه</span><input name="password" type="password" dir="ltr" minLength={12} required/></label><label><span>نقش</span><select name="role"><option value="admin">ادمین</option><option value="seo">سئوکار</option><option value="manager">مدیریت</option></select></label><button>ایجاد کاربر</button></form></section><section className="admin-staff-list"><div className="admin-table-head"><span>کاربر</span><span>نقش</span><span>وضعیت</span><span>آخرین فعالیت</span></div>{data?.length?data.map(item=><article key={item.id}><div><strong>{item.display_name||"بدون نام نمایشی"}</strong><small dir="ltr">{item.phone||item.id.slice(0,8)}</small></div><span>{roleLabel(item.role)}</span><i className={item.is_active?"active":""}>{item.is_active?"فعال":"غیرفعال"}</i><time>{item.last_seen_at?new Intl.DateTimeFormat("fa-IR-u-ca-persian",{dateStyle:"medium",timeZone:"Asia/Tehran"}).format(new Date(item.last_seen_at)):"—"}</time></article>):<div className="admin-module-empty"><h2>هنوز کاربری تعریف نشده است</h2></div>}</section></main>}
