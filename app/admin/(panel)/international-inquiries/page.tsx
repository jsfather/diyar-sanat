import{requireStaff}from"@/lib/admin/auth";import{createClient}from"@/lib/supabase/server";import{updateInternationalInquiry}from"./actions";type Props={searchParams:Promise<Record<string,string|undefined>>};const labels={new:"جدید",reviewing:"در حال بررسی",needs_information:"نیازمند اطلاعات",approved:"تأییدشده",rejected:"ردشده",archived:"بایگانی"}as const;
export default async function InternationalInquiriesPage({searchParams}:Props){const[,q]=await Promise.all([requireStaff(),searchParams]),{data=[]}=await(await createClient()).from("international_inquiries").select("id,tracking_code,company_name,country,website,business_field,import_distribution_experience,interested_products,estimated_volume,cooperation_type,status,internal_note,created_at").order("created_at",{ascending:false});return <main className="admin-module-page"><header><div><div><small>توسعه بازار</small><h1>درخواست‌های همکاری بین‌المللی</h1><p>ارزیابی پیشنهاد شرکت‌ها بدون نمایش عمومی اطلاعات</p></div></div></header>{q.saved?<p className="admin-alert success">وضعیت درخواست ذخیره شد.</p>:null}{q.error?<p className="admin-alert error">ذخیره تغییرات انجام نشد.</p>:null}<section className="admin-application-list">{data?.length?data.map(item=><details className="admin-application-item" key={item.id}><summary><div><strong>{item.company_name}</strong><small>{item.country} · {item.business_field}</small></div><span dir="ltr">{item.tracking_code}</span><i data-status={item.status}>{labels[item.status]}</i></summary><div className="admin-application-details"><dl><dt>وب‌سایت</dt><dd dir="ltr">{item.website??"—"}</dd><dt>نوع همکاری</dt><dd>{item.cooperation_type}</dd><dt>محصولات</dt><dd>{item.interested_products}</dd><dt>حجم تقریبی</dt><dd>{item.estimated_volume??"—"}</dd><dt>سابقه</dt><dd>{item.import_distribution_experience}</dd></dl><form action={updateInternationalInquiry}><input type="hidden" name="id" value={item.id}/><label><span>وضعیت</span><select name="status" defaultValue={item.status}>{Object.entries(labels).map(([value,label])=><option key={value} value={value}>{label}</option>)}</select></label><label><span>یادداشت داخلی</span><textarea name="internal_note" rows={4} defaultValue={item.internal_note??""}/></label><button>ذخیره بررسی</button></form></div></details>):<div className="admin-catalog-empty"><strong>درخواستی ثبت نشده است</strong></div>}</section></main>}
